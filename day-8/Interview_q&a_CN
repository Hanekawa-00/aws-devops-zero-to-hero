# EC2、IAM 和 VPC 场景面试题

Q: 你被分配为两层应用程序设计 VPC 架构。该应用程序需要高可用和可扩展。你会如何设计 VPC 架构？

A: 在这种场景下，我会按以下方式设计 VPC 架构。
   我会创建 2 个子网：公有子网和私有子网。公有子网包含负载均衡器并可从互联网访问。私有子网托管应用服务器。
   我会将子网分布到多个可用区以实现高可用性。此外，我会为应用服务器配置自动伸缩组。

Q: 你的组织有一个包含多个子网的 VPC。你想限制一个子网中资源的出站互联网访问，但允许另一个子网中资源的出站互联网访问。你会如何实现？

A: 要限制一个子网中资源的出站互联网访问，我们可以修改与该子网关联的路由表。在路由表中，我们可以删除指向互联网网关的默认路由 (0.0.0.0/0)。
   这将阻止该子网中的资源访问互联网。对于需要出站互联网访问的子网，我们可以保留指向互联网网关的默认路由。

Q: 你有一个包含公有子网和私有子网的 VPC。私有子网中的实例需要访问互联网进行软件更新。你会如何允许私有子网中的实例访问互联网？

A: 要允许私有子网中的实例访问互联网，我们可以使用 NAT 网关或 NAT 实例。
   我们会将 NAT 网关/实例放置在公有子网中，并配置私有子网路由表将出站流量发送到 NAT 网关/实例。这样，私有子网中的实例可以通过 NAT 网关/实例访问互联网。

Q: 你在 VPC 中启动了 EC2 实例，希望它们使用私有 IP 地址相互通信。你会采取什么步骤来启用这种通信？

A: 默认情况下，同一 VPC 内的实例可以使用私有 IP 地址相互通信。
  要确保这种通信，我们需要确保实例在同一 VPC 中启动，并放置在同一子网或通过对等连接或 VPC 对等链接连接的子网中。
  此外，我们应该检查与实例关联的安全组，确保配置了必要的入站和出站规则以允许它们之间的通信。

Q: 你想为 VPC 资源实施严格的网络访问控制。你会如何实现？

A: 要为 VPC 资源实施精细的网络访问控制，我们可以使用网络访问控制列表 (NACL)。
  NACL 是无状态的，在子网级别运行。我们可以在 NACL 中定义入站和出站规则，根据源和目标 IP 地址、端口和协议允许或拒绝流量。
  通过仔细配置 NACL 规则，我们可以对进入和离开子网的流量实施精细的访问控制。

Q: 你的组织需要在 VPC 中建立一个隔离环境来运行敏感工作负载。你会如何设置这个隔离环境？

A: 要在 VPC 中设置隔离环境，我们可以创建一个不连接互联网网关的子网。
   这个子网称为"隔离子网"，不会有直接的互联网连接。我们可以将敏感工作负载放置在此子网中，确保它们免受入站和出站互联网流量的影响。
   但是，如果这些工作负载需要出站互联网访问，我们可以在不同的子网中设置 NAT 网关或 NAT 实例，并配置隔离子网的路由表将出站流量通过 NAT 网关/实例发送。

Q: 你的应用程序需要在 VPC 内安全地访问 AWS 服务，如 S3。你会如何实现？

A: 要在 VPC 内安全访问 AWS 服务，我们可以使用 VPC 端点。VPC 端点允许 VPC 中的实例私下与 AWS 服务通信，无需互联网网关或 NAT 网关。
  我们可以为特定 AWS 服务（如 S3 和 DynamoDB）创建 VPC 端点，并将它们与 VPC 关联。
  这使 VPC 中的实例与 AWS 服务之间能够进行安全高效的通信。

Q: NACL 和安全组有什么区别？用一个案例解释？

A: 例如，我想设计一个安全架构，我会结合使用 NACL 和安全组。在子网级别，我会配置 NACL 以根据源和目标 IP 地址、端口和协议强制执行入站和出站流量限制。NACL 是无状态的，可以在子网边界过滤流量，提供额外的防御层。
  在实例级别，我会利用安全组来控制入站和出站流量。安全组是有状态的，在实例级别运行。通过仔细定义安全组规则，我可以根据应用程序的安全要求允许或拒绝进出实例的特定流量。
  通过结合 NACL 和安全组，我可以在网络和实例级别实现精细的安全控制，为敏感应用程序提供深度防御。

Q: IAM 用户、组、角色和策略有什么区别？

A: IAM 用户：IAM 用户是 AWS 中的身份，代表需要访问 AWS 资源的个体或应用程序。IAM 用户有永久长期凭证，如用户名和密码，或访问密钥（访问密钥 ID 和秘密访问密钥）。IAM 用户可以直接分配 IAM 策略，或添加到 IAM 组以便更容易管理权限。
   IAM 角色：IAM 角色类似于 IAM 用户，但不与特定个体关联。相反，它由 IAM 用户、应用程序或服务等实体假定以获取临时安全凭证。当你想向 AWS 账户外部的实体授予权限，或想在账户之间委托 AWS 资源访问权限时，IAM 角色很有用。IAM 角色附加了策略，定义了假定角色时授予的权限。
   IAM 组：IAM 组是 IAM 用户的集合。通过将 IAM 用户组织到组中，你可以集中管理权限。IAM 组使同时向多个用户分配权限更容易。IAM 组内的用户继承分配给该组的权限。例如，你可以创建"开发者"组并分配适当的策略以授予组织中开发者所需的权限。
   IAM 策略：IAM 策略是在 AWS 中定义权限和访问控制的文档。IAM 策略可以附加到 IAM 用户、IAM 角色和 IAM 组，以定义可以在哪些 AWS 资源上执行什么操作。IAM 策略使用 JSON（JavaScript 对象表示法）语法指定权限，可以独立于用户、角色或组创建和管理。IAM 策略由语句组成，包括允许或拒绝的操作、可以执行操作的资源，以及任何附加条件。

Q: 你的 VPC 中有一个私有子网，包含多个不应直接访问互联网的实例。但你仍然需要能够安全地访问这些实例进行管理。你会如何设置堡垒主机来实现这种访问？

A: 要安全访问私有子网中的实例，你可以设置堡垒主机（也称为跳板主机或跳板机）。堡垒主机充当私有子网的安全入口点。以下是设置堡垒主机的方法：
      在公有子网中创建一个新的 EC2 实例，作为堡垒主机。确保此实例有公有 IP 地址或关联弹性 IP 地址以便持久访问。
      配置堡垒主机的安全组，允许来自你的 IP 地址或受限可信 IP 地址范围的入站 SSH（或 Windows 的 RDP）流量。这限制只有授权管理员才能访问堡垒主机。
      将实例放置在私有子网中，并配置它们的安全组允许来自堡垒主机安全组的入站 SSH（或 RDP）流量。
      使用私钥或密码 SSH（或 RDP）到堡垒主机。从堡垒主机，你可以使用私有 IP 地址 SSH（或 RDP）到私有子网中的实例。