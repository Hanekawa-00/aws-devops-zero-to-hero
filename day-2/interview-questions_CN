# 面试问题

问: 什么是 AWS IAM，为什么它很重要？

答: AWS IAM (身份与访问管理) 是 Amazon Web Services 提供的一项服务，帮助你控制对 AWS 资源的访问。它允许你管理用户身份、权限和策略。IAM 很重要，因为它通过确保只有授权的个人或实体才能访问你的 AWS 资源来增强安全性，帮助你执行最小权限原则并维护安全的环境。

问: IAM 用户和 IAM 角色有什么区别？

答: IAM 用户代表需要访问你的 AWS 资源的个人人员或实体。他们有自己的凭证，通常与长期访问相关联。另一方面，IAM 角色用于授予对 AWS 资源的临时访问权限，通常用于应用程序或服务。角色具有关联的策略，可以由受信任的实体代入以安全地访问资源。

问: 什么是 IAM 策略，它们如何工作？

答: IAM 策略是定义权限的 JSON 文档。它们指定在 AWS 资源上允许或拒绝的操作，可以附加到 IAM 用户、组或角色。策略通过将用户或实体请求的操作与策略中允许或拒绝的操作进行匹配来控制访问。如果请求的操作与策略中的允许操作匹配，则授予访问权限；否则，拒绝访问。

问: 什么是最小权限原则，为什么它在 IAM 中很重要？

答: 最小权限原则规定，用户应仅获得执行其任务所需的权限，不应更多。它在 IAM 中很重要，因为它最大限度地减少了未经授权访问的风险，并限制了被入侵账户可能造成的潜在损害。遵循最小权限原则有助于维护安全的环境，确保用户仅拥有执行其工作职责所需的权限。

问: 什么是 AWS 管理策略？

答: AWS 管理策略是由 AWS 创建和管理的预定义策略。这些策略涵盖常见用例，并为特定 AWS 服务或操作提供预定义权限。AWS 管理策略由 AWS 维护和更新，确保它们与新的 AWS 服务和功能保持同步。它们可以附加到你 AWS 账户中的 IAM 用户、组或角色。